import { Dimensions, StyleSheet } from 'react-native';

const styles = StyleSheet.create({
	border: {
		borderColor: 'black',
		borderWidth: 1,
		marginTop: 50,
		width: '100%',
	},
	container: {
		flex: 3,
	},
	inputContainer: {
		flex: 1,
		paddingVertical: 20,
	},
});

export default styles;
